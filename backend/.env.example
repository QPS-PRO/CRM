# Generate a secure key: python3 -c "import secrets; print(secrets.token_urlsafe(50))"
# IMPORTANT: Never commit the actual SECRET_KEY to version control!
SECRET_KEY=5MfNKYozw6Dv3wyzm0pfgddjnsTS5E4L8TmuJ3PpsMc5v-4-6RV6pU7JpXwuNJN3ie0

# DEBUG: Enable/disable debug mode
# Set to False in production for security
DEBUG=True

# ALLOWED_HOSTS: Comma-separated list of allowed hostnames
# For development: localhost,127.0.0.1
# For production: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# TIME_ZONE: Django timezone setting
# Examples: Africa/Cairo, America/New_York, UTC
TIME_ZONE=Asia/Riyadh

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL database connection settings
DB_NAME=schoolhub
DB_USER=schoolhub_user
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# CORS_ALLOWED_ORIGINS: Comma-separated list of allowed origins for CORS
# For development: leave empty (CORS_ALLOW_ALL_ORIGINS will be True if DEBUG=True)
# For production: add your frontend URLs (comma-separated, no spaces)
# Example: https://app.yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# -----------------------------------------------------------------------------
# Celery Configuration
# -----------------------------------------------------------------------------
# Celery broker and result backend URLs
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# -----------------------------------------------------------------------------
# SMS Configuration (Optional - Mora SMS API)
# -----------------------------------------------------------------------------
SMS_ENABLED=True
SMS_API_KEY=c61555ce0f3e446722a748b1a6280df0ea0782d7
SMS_USERNAME=badr2025
SMS_SENDER_NAME=QurtubahJed
SMS_SENDER_NUMBER=+966555027448

